import React from 'react';
import ReactDOM from 'react-dom/client';
import LumomireApp from './LumomireApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LumomireApp />
  </React.StrictMode>
);
